# P14.23 — JSON archive cleanup

**Date:** 2026-08-28  
**Scope:** P14.22 is GO. PostgreSQL is the only catalog store. This slice **verifies** `.p14.17-json-archive/` against the P14.17 record, proves production code does not read it, then **deletes that directory only**. It does **not** delete `nac-demo.json`, does **not** mutate PostgreSQL, and does **not** start P15.

```text
P14.21 post-cutover verification     GO
   ↓
P14.22 remove dual-write remnants    GO
   ↓
P14.23 JSON archive cleanup          ← this document
   ↓
P15.1 production PostgreSQL audit    (separate slice; needs its own GO)
```

---

## Command

```bash
npm run db:delete-json-archive
```

Refuses unless the write-freeze is lifted, the P14.21 marker is valid, PostgreSQL is reachable with `001_current_schema.sql`, the four catalog repositories read from PostgreSQL, `data/` has no catalog JSON, `nac-demo.json` is present, the archive sits outside `data/` and still matches the P14.17 MANIFEST (timestamp, counts, fingerprints), and the four catalog repositories do not mention the archive. Does **not** INSERT/UPDATE/DELETE PostgreSQL. Does **not** delete `nac-demo.json`.

---

## P14.17 record this slice compares against

| Field | Expected |
| --- | --- |
| `archivedAt` | `2026-08-26T19:58:01.517Z` |
| Archive merchants | **205** |
| Archive workspaces | **5** |
| Archive events | **139** |
| Archive guard-decisions | **153** |
| `nac-demo.json` in archive | **absent** |

PostgreSQL guard count can be higher than 153 because of the documented 10 PG-only rows. That is **not** a reason to keep the archive.

---

## Sequence

```text
Require freeze lifted + P14.21 marker
  ↓
PostgreSQL reachable; migration 001 applied
  ↓
Repository SELECT smoke (merchants / workspaces / events / guards)
  ↓
data/ catalog JSON absent; nac-demo.json present
  ↓
Archive outside data/; required catalogs present; fingerprints match MANIFEST
  ↓
Catalog repos have no .p14.17-json-archive reference
  ↓
Integrity fail → refuse; do not delete
  ↓
Delete .p14.17-json-archive/ only
  ↓
nac-demo.json fingerprint unchanged; PG counts unchanged; catalogs still absent
```

---

## Not in this slice

- Delete `nac-demo.json` or `demo.repository.ts`
- Delete migrations or PostgreSQL rows
- Remove operator freeze / backfill CLIs
- API / Auth / Guard policy / Financial Engine changes
- Full `npm test` against live `DATABASE_URL`
- P15

Isolated tests use a temp `.p14.17-json-archive` for the delete helper. They do not point `DATABASE_URL` at live catalogs.

---

## Live run (this workstation, 2026-08-28)

`npm run db:delete-json-archive` verified fingerprints against the P14.17 MANIFEST, then deleted **only** `.p14.17-json-archive/`.

| Check | Result |
| --- | --- |
| Isolated `tests/p14.23-archive-cleanup.test.ts` (+ P14.18–P14.22) | **31/31 pass** before delete |
| Archive integrity vs P14.17 | **PASS** (`archivedAt` `2026-08-26T19:58:01.517Z`; counts 205 / 5 / 139 / 153; fingerprints matched MANIFEST) |
| PostgreSQL `COUNT(*)` before | merchants **205**, workspaces **5**, track_events **139**, guard_decisions **163** |
| PostgreSQL `COUNT(*)` after | merchants **205**, workspaces **5**, track_events **139**, guard_decisions **163** |
| Deleted | `.p14.17-json-archive/` only |
| `data/nac-demo.json` | present, fingerprint unchanged |
| `data/` catalog JSON | still absent |
| Marker | `data/.p14.23-json-archive-deleted` (`deletedAt` `2026-08-28T18:34:11.581Z`) |
| Isolated tests after delete (P14.18–P14.23) | **31/31 pass** |
| `npm run backend:typecheck` | pass |
| `npx next build` | pass |
| Full `npm test` | **not run** |
| P15 | **not started** |

**Verdict: P14.23 JSON archive cleanup GO.** The archive matched the P14.17 record and was deleted. PostgreSQL was not mutated. `nac-demo.json` remains.
