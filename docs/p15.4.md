# P15.4 — Final Hardening / Verification

**Date:** 2026-08-29  
**Scope:** Verify the catalog after the P15.3 targeted deletes. This slice does **not** add FK or UNIQUE(phone), does **not** create migration `002`, does **not** delete more rows, and does **not** start Final Audit or P16.

```text
P15.3 P12.2 workspace delete              GO
   ↓
P15.4 Final Hardening / Verification      ← this document  GO
   ↓
        later Final Audit (needs its own GO)
```

**Verdict: GO.** Counts, orphans, schema, probe rollback, and query plans all match. PostgreSQL was not mutated.

---

## Command

```bash
npm run db:p15.4-verify
```

SELECT for counts, orphans, constraints, indexes, and EXPLAIN. A four-table write probe runs inside one transaction and is **always ROLLBACK**. Writes `data/.p15.4-final-hardening`. Do not run `db:backfill` or `db:reconcile`.

---

## Not in this slice

- FK / UNIQUE(phone) / CHECK
- Migration `002_*.sql`
- Further DELETE of catalog leftovers
- Workspace compare-and-swap
- Final Audit
- P16
- Cleanup of P14/P15 operator tools
- Full `npm test` against live `DATABASE_URL`

---

## Live run (this workstation, 2026-08-29)

`evaluatedAt` **2026-08-29T14:58:33.525Z**. Probe **ROLLBACK**. `postgresMutated` **false**.

| Check | Result |
| --- | --- |
| Isolated P14.4–P15.4 | **177/177 pass** |
| `npm run backend:typecheck` | pass |
| `npx next build` | pass (existing json-store / write-freeze tracing warnings) |
| Catalog counts | merchants **205**, workspaces **3**, track_events **181**, guard_decisions **115** |
| Workspace / guard / event orphans | **0 / 0 / 0** |
| Remaining workspaces | `pitch.demo.1786709548@smartprofits.dev`, `pitch.demo.1786709716@smartprofits.dev`, `tsraathmd@gmail.com` |
| FK / extra UNIQUE | **0 / none** |
| `schema_migrations` | `001_current_schema.sql` only |
| Migration `002` | **absent** |
| Unfiltered `guard_decisions ORDER BY created_at DESC LIMIT 50` | **Seq Scan**, cost **35.26** |
| `guard_decisions WHERE email` | **Index Scan** (`guard_decisions_email_created_idx`) |
| `merchants WHERE email` | **Index Scan** (PK) |
| `createIndexNow` | **false** (115 rows, below 10_000) |
| Write probe | INSERT 4 catalog rows visible inside tx, then ROLLBACK |
| Probe leftover | **0** |
| Rows deleted | **0** |
| Catalog repos JSON fallback | **none** (`requirePostgres` throws) |
| Final Audit / P16 | **not started** |

### Counts (unchanged by the probe)

| Table | Expected | Before probe | After ROLLBACK |
| --- | ---: | ---: | ---: |
| merchants | 205 | 205 | 205 |
| workspaces | 3 | 3 | 3 |
| track_events | 181 | 181 | 181 |
| guard_decisions | 115 | 115 | 115 |

### Indexes (public)

`guard_decisions_pkey`, `guard_decisions_email_created_idx`, `guard_decisions_decision_created_idx`, `merchants_pkey`, `workspaces_pkey`, `track_events_pkey`, `track_events_at_idx`, `schema_migrations_pkey`.

No created_at-only index. No phone UNIQUE.

Still deferred (need a later GO): UNIQUE(phone), any FK, migration 002, workspace CAS, Final Audit, P14/P15 tool cleanup.

---

## Isolated tests

`tests/p15.4-final-hardening.test.ts` classifies fixture evidence in-process, fakes the probe ROLLBACK, and locks catalog repositories against JSON fallback. It does not import a live `pg.Pool`.
