# P15.6 — Operator-tool cleanup

**Date:** 2026-08-30  
**Scope:** Remove unused P14/P15 cutover tools after the P15.5 Final Audit GO. No PostgreSQL writes. No migration. No FK. No UNIQUE. No API/Auth/Guard/Engine changes. No P16. This is **not** Final Verification.

```text
P15.5 Final Audit                         GO
   ↓
P15.6 Operator-tool cleanup               ← this document
   ↓
        later Final verification
   ↓
        later P16
```

**Verdict:** Unused cutover CLIs and their lock tests were removed. Runtime, migrate, P15 audit/verify/delete locks, `write-freeze`, `json-store` / `nac-demo.json`, and historical docs stayed.

---

## Audit (before delete)

- **Production / runtime:** catalog repositories import only `server/db/postgres`. Smart Guard imports `isWriteFrozenError` from `server/write-freeze.ts`. Demo stays on `nac-demo.json`.
- **CI:** no `.github` workflows. Isolated tests are `package.json` `"test"` only.
- **Operational scripts kept:** `db:up` / `db:down` / `db:migrate`, `scripts/start-persistent-postgres.ts`, `scripts/ensure-smartprofit-db.ts`, P15.1–P15.5 CLIs.

A file was deleted only when every runtime, CI, and current operational script reference was gone (or was the tool’s own lock test / npm script).

**If usage was unclear → KEEP.** That includes P15.3 one-shot delete CLIs (locked by current tests; re-running them would mutate PostgreSQL), `postgres-audit-samples-cli.ts` (documented in P15.1), `write-freeze.ts` (Smart Guard + test isolation), and all `docs/p14*` / `docs/p15*` history.

---

## PostgreSQL

Not connected for this slice. No INSERT / UPDATE / DELETE. Schema still `001_current_schema.sql` only.

---

## Deleted (55 files)

Cutover CLIs and modules that had no runtime, CI, or current operational import:

- `server/db/backfill.ts`, `backfill-cli.ts`, `backfill-validate.ts`, `cli-data-dir.ts`
- `post-backfill-verify.ts`, `post-backfill-verify-cli.ts`
- `reconcile.ts`, `reconcile-cli.ts`, `reconcile-frozen-cli.ts`, `reconcile-p14.15-pre-cutover-cli.ts`, `reconcile-p14.15-plan.ts`
- `freeze-plan.ts`, `freeze-cli.ts`, `unfreeze-cli.ts`, `json-snapshot.ts`
- `shadow-validate.ts`, `shadow-validate-cli.ts`
- `archive-json-shadow.ts`, `archive-json-shadow-cli.ts`, `archive-delete.ts`, `archive-delete-cli.ts`
- `cutover-cli.ts`, `final-cutover.ts`, `final-cutover-cli.ts`, `post-cutover.ts`, `post-cutover-cli.ts`
- `final-verify.ts`, `final-verify-cli.ts`, `operational-verify.ts`, `operational-verify-cli.ts`
- `intentional-pg-only-guards.ts`, `test-artifact-pg-only-guards.ts`, `track-event-identity.ts`
- `scripts/pg-counts.ts`, `p14.12-preflight.ts`, `p14.12-preflight-detail.ts`, `freeze-json-snapshot.ts`, `diagnose-guard-created-at.ts`
- Lock tests of those tools: `tests/p14.8` through `p14.11`, `p14.12r`, `p14.13`, `p14.14`, `p14.14r`, `p14.15-postgres-primary-cutover`, `p14.15-pre-cutover`, `p14.16`, `p14.17`, `p14.19`, `p14.20`, `p14.21`, `p14.23`, and `tests/helpers/p14.9-dry-run.ts`

---

## Kept (why)

| Item | Why KEEP |
| --- | --- |
| `server/db/postgres.ts`, `migrate.ts` / `migrate-cli.ts`, `001_current_schema.sql` | Runtime + operational migrate |
| Catalog repositories, API, Auth, Guard, Engine | Production |
| `server/write-freeze.ts` | Smart Guard `isWriteFrozenError` + test isolation |
| `json-store.ts`, `nac-demo.json`, `demo.repository.ts` | Demo store; not catalog |
| `lib/utils.ts` | Product constraint |
| `scripts/start-persistent-postgres.ts`, `ensure-smartprofit-db.ts` | Current operational |
| P15.1–P15.5 modules/CLIs and their tests | Current locks; some one-shot deletes are dangerous if removed from the lock set |
| `postgres-audit-samples-cli.ts` | Documented in P15.1 |
| `p14.23-archive-marker.ts` | P15.1/P15.2 still gate on the archive-deleted marker |
| `p14.4`, `p14.5`, `p14.6`, `p14.7`, `p14.12`, `p14.15-test-storage-isolation`, `p14.18`, `p14.22` tests | Lock current runtime, migrate, or test isolation |
| All `docs/p14*` / `docs/p15*` | History, not tools |

---

## References updated

- `package.json`: removed `db:backfill`, `db:verify`, `db:verify-final`, `db:freeze`, `db:unfreeze`, `db:reconcile`, `db:reconcile-frozen`, `db:reconcile-pre-cutover`, `db:cutover`, `db:validate-shadow`, `db:archive-and-remove-json`, `db:verify-operational`, `db:final-cutover`, `db:verify-post-cutover`, `db:delete-json-archive`. Isolated `test` list no longer includes the deleted lock tests; added `tests/p15.6-cleanup.test.ts`.
- `server/db/postgres-audit-cli.ts` and `orphan-classify-cli.ts`: marker parse now from `p14.23-archive-marker.ts`.
- `server/db/postgres-audit.ts`: exported `catalogSourceIsPostgresOnly` (unused leftover of the P14.22 import experiment; P14.22 inlines the helper so the test still does not import `@/server/db/postgres*`).
- `tests/p14.22-remove-dual-write-remnants.test.ts`: `OPERATOR_KEPT` no longer lists deleted CLIs.
- `tests/p15.5-final-audit.test.ts`: `backfill-cli.ts` is now expected absent.

---

## Checks

- PostgreSQL: not mutated (no connection in this slice)
- `npm run backend:typecheck`: pass
- Isolated architecture + engine + remaining P14/P15 tests: **224/224 pass**
- `next build` / P16: **not started**
