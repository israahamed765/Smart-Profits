# P14.11 — Post-Backfill Verification (Persistent Recovery)

**Date:** 2026-08-26  
**Scope:** Independent check that PostgreSQL matches a **frozen JSON snapshot**. **JSON remains primary. No production logic change. No auto-repair of rows. No P14.12.**

```text
P14.10 Controlled Backfill     session copy (later unavailable)
   ↓
P14.11 first pass              NO-GO (ECONNREFUSED / no standing DB)
   ↓
P14.11 recovery                persistent cluster + snapshot copy + verify  ← this document
   ↓
        GO / NO-GO
```

**First pass verdict: NO-GO** — environment only (`ECONNREFUSED`). Not a JSON↔PG payload mismatch.

**Recovery verdict: GO** — snapshot `192 / 5 / 125 / 130` matches PostgreSQL. PostgreSQL is **not** primary.

---

## Phase A — Environment

| Check | Result |
| --- | --- |
| `npm run db:up` (docker-compose) | **failed** — `docker` is not on PATH |
| Intended project setup | `docker-compose.yml` volume `smartprofit_pg`, Postgres 16, port 5432 |
| What ran instead | Durable `pg_ctl` daemon, data at `%USERPROFILE%\.smartprofit-pg` (ASCII path; the Arabic project path breaks `initdb --encoding=UTF8`) |
| Session / `persistent: false` cluster | **not used** |
| `package.json` scripts | **unchanged** (`db:up` / `db:migrate` / `db:backfill` / `db:verify`) |
| `npm run db:migrate` | applied **`001_current_schema.sql`** |
| `schema_migrations` | present |

### PG counts **before** any INSERT

| merchants | workspaces | track_events | guard_decisions |
| ---: | ---: | ---: | ---: |
| **0** | **0** | **0** | **0** |

Empty catalog → no existing-row conflict. No DELETE.

---

## Phase B — Frozen snapshot

Live `data/` was **copied, not rewritten**, to `.p14.11-snapshot/` (gitignored; contains password hashes). Backfill/verify used `--data-dir .p14.11-snapshot`.

Frozen at: `2026-08-26T13:59:32.382Z`

| Entity | Count |
| --- | ---: |
| merchants | **192** |
| workspaces | **5** |
| track_events | **125** |
| guard_decisions | **130** |

Checksums (sha256):

| File | bytes | sha256 |
| --- | ---: | --- |
| users.json | 96069 | `43c949c4cc2351097a05e30919314592b8e0f2cf611ac4be67837f6bfb638131` |
| events.json | 7529 | `5211ee734c0ccb2d09d71bb66c568d5e54ca46313068e83f331d2107f6375025` |
| guard-decisions.json | 252163 | `507d84de73520f39637460f58d5719041f4fb07a2c417a31fd8e51985c4c8cc0` |
| nac-demo.json | 3092 | `829c1fd9ca671cac92f3c543863c157b8d4fef002cef679e1ab412e1bcef0dd8` |
| workspaces/p12.2-alice_test.com.json | 838 | `0cc72b2adbd7240e9d18d13c6e2987b2120d767278fc1f285e25ebd8bb1bdc9f` |
| workspaces/p12.2-bob_test.com.json | 806 | `399c3cee94418feb6fab4098dfb5aeec57add5db1d0f907abf9ef3119fb8a873` |
| workspaces/pitch.demo.1786709548_smartprofits.dev.json | 118996 | `a6ef272d8cccb453a69c61ed68927f83bc1addeb151bd9e1d6fa1b80bd629b52` |
| workspaces/pitch.demo.1786709716_smartprofits.dev.json | 118996 | `0c5f732e9c33e7a7ef8a9078de7ae25b90b04ac5e07c427c29ccbd9830dec79c` |
| workspaces/tsraathmd_gmail.com.json | 1177361 | `a80114bae96f3b198e7b83db8f1255ca634c0cd2ea0d7394954a64bd517ffacd` |

---

## Phase C — Controlled backfill (`npm run db:backfill -- --data-dir .p14.11-snapshot`)

Dry-run JSON conflicts: **0**. Event ids: `sha256_hex(NUL-join("smartprofit.track_event.v1", at, type, label, email))`. `ON CONFLICT DO NOTHING`. No `randomUUID()` in the copier. No DELETE.

| Entity | JSON source | Inserted | Skipped | PG after |
| --- | ---: | ---: | ---: | ---: |
| merchants | 192 | **192** | 0 | 192 |
| workspaces | 5 | **5** | 0 | 5 |
| track_events | 125 | **125** | 0 | 125 |
| guard_decisions | 130 | **130** | 0 | 130 |

Warnings only (copied as-is): 2 merchants missing password; 2 orphaned workspaces; 29 orphaned guard rows.

---

## Phase D — Verification (`npm run db:verify -- --data-dir .p14.11-snapshot`)

First comparator pass reported `guards.createdAtMismatch = 129`. Diagnosis (SELECT only): all **130** rows are the **same instant** as JSON; `node-pg` returns a `Date`, and `Date.parse(String(date))` fails on the Arabic timezone label. **No row was updated.** The comparator now compares epoch milliseconds. Re-verify: **GO**.

| Check | Result |
| --- | --- |
| snapshot counts = PG counts | **192 / 5 / 125 / 130** |
| merchants missing / extra | **0 / 0** |
| `payload.email === email` | **0** mismatches |
| password presence (not values) | JSON 190 = PG 190 |
| workspaces missing / extra / payload / files.length | **0** |
| guard ids missing / extra | **0 / 0** |
| event sha256 recompute | **0** `idNotDeterministic` |
| duplicates | **0** |

### Idempotency

Inside `db:verify` and a second `db:backfill`:

| Entity | Inserted | Skipped | PG counts |
| --- | ---: | ---: | ---: |
| merchants | **0** | 192 | 192 |
| workspaces | **0** | 5 | 5 |
| guards | **0** | 130 | 130 |
| events | **0** | 125 | 125 |

---

## Phase E — Regression

Unchanged: dual-write P14.4 order, `appendEvent` still `randomUUID()`, PostgreSQL is not primary, API / Auth / Guard / Financial Engine / Frontend not modified. Snapshot and live `data/` checksums **unchanged** during verify (`liveData.unchanged: true`).

### Gates (this workstation, 2026-08-26)

| Command | Result |
| --- | --- |
| `npm run db:verify -- --data-dir .p14.11-snapshot` | **GO** (exit 0) |
| `npm test` | **298 passed / 0 failed** |
| `npm run backend:typecheck` | **pass** |
| `npx next build` | **pass** (existing json-store tracing warnings only) |

---

## Remaining risks

1. Docker Desktop is still missing. The durable cluster is `pg_ctl` under the user profile, not `docker compose`. Restart: `npx tsx --env-file=.env scripts/start-persistent-postgres.ts`.
2. JSON can still grow after this freeze; live `data/` is primary. New dual-write events still mint UUID ids.
3. `embedded-postgres` binaries are copied to `%USERPROFILE%\.smartprofit-pg-dist` (not a package.json dependency).
4. Do **not** start P14.12 without a new explicit approval.

---

## DO NOT (P14.11)

- PostgreSQL-primary
- Change repositories / dual-write / `appendEvent`
- Delete JSON
- Auto-repair by DELETE/UPDATE when catalogs disagree
- Start P14.12 without a new explicit approval
