# P15.7 — Final Verification

**Date:** 2026-08-30  
**Scope:** Read-only close gate after Cleanup. No product changes. No P16.

```text
P15.6 Operator-tool cleanup               GO
   ↓
P15.7 Final Verification                  ← this document  GO
   ↓
        later P16 (needs its own GO)
```

**Verdict: FINAL VERIFICATION: GO.**

PostgreSQL was not mutated. Schema is still `001` only. No FK. No UNIQUE(phone). `docs/p16.md` is absent.

---

## Gates

| # | Check | Result |
| --- | --- | --- |
| 1 | PostgreSQL connectivity | reachable |
| 2 | Schema / `schema_migrations` | public tables match; **001_current_schema.sql** only |
| 3 | Counts | merchants **205**, workspaces **3**, track_events **181**, guard_decisions **115** |
| 4 | Orphans / payload mismatches | **0 / 0 / 0** orphans; payload integrity **0** |
| 5 | Catalog repositories | PostgreSQL-only; `requirePostgres`; no JSON fallback |
| 6 | `nac-demo.json` | still JSON-only via `demo.repository` |
| 7 | Cleanup leftovers | no broken imports of deleted tools |
| 8 | `package.json` scripts | all `db:*` targets exist; cutover scripts remain absent |
| 9 | Isolated P14/P15 tests | **106/106 pass** |
| 10 | `backend:typecheck` | pass |
| 11 | `npx next build` | pass (existing json-store / write-freeze tracing warnings) |
| 12 | Smoke | GET `/api/auth/me` **401**, `/api/workspace` **401**, `/login` **200**, `/api/smart-guard/logs` **401**; four-table INSERT probe visible then **ROLLBACK**; leftover **0**; repository SELECTs 205/3 |

Live SELECT `evaluatedAt` **2026-08-30T09:41:56.016Z**. Probe leftover **0**. `postgresMutated` **false**.

---

## Not started

P16. UNIQUE(phone) and FKs remain optional and were not added.
