# P14.12 — PostgreSQL Primary Read Cutover

**Date:** 2026-08-26  
**Scope:** Preflight only. **No repository read-order change. No JSON deletion. Dual-write unchanged. No P14.13.**

```text
P14.11 Post-Backfill Verification   GO (frozen snapshot 192/5/125/130)
   ↓
P14.12 PostgreSQL Primary Read      ← this document
   ↓
        GO / NO-GO
```

**Verdict: NO-GO**

PostgreSQL is reachable and `001_current_schema.sql` is applied. The P14.11 freeze is **no longer** the live catalog: dual-write continued after that GO. A SELECT-only compare of **live JSON vs live PostgreSQL** failed. Repositories were **not** switched to PostgreSQL-primary reads.

No silent repair. No DELETE. No backfill during this preflight.

---

## Preflight (live, SELECT-only)

| Check | Required | Observed |
| --- | --- | --- |
| PostgreSQL reachable | yes | **yes** |
| `001_current_schema.sql` | applied | **applied** |
| Counts vs P14.11 freeze | 192 / 5 / 125 / 130 | **193 / 5 / 127 / 130** (PG) |
| Live JSON counts | — | **193 / 5 / 127 / 135** |
| 0 missing / extra / duplicates vs freeze | yes | **fail** (live catalog drifted) |
| Live JSON vs PG | 0 missing / extra | **fail** (below) |

### Live mismatches (not repaired)

| Store | Issue |
| --- | --- |
| merchants | counts match **193 / 193**, 0 missing / extra / duplicate emails |
| workspaces | counts **5 / 5**; payload differs (`savedAt`/`files` and/or `settings`/`actionLog`/`taxonomy`) |
| track_events | counts **127 / 127**, but **2** PG ids are `randomUUID()` (post-backfill `appendEvent`) and **2** JSON events are missing as sha256 ids; **2** PG ids fail `id === sha256(payload)` |
| guard_decisions | JSON **135**, PG **130**, **5** JSON-only ids (PG-unavailable write fell back to file; expected P14.4 split, still a cutover blocker) |

Event identity mix is the locked P14.4 write path (`appendEvent` still `randomUUID()`) on top of P14.10 sha256 backfill. Guard JSON-ahead-of-PG is the locked “PG insert null → file” path.

---

## What was not done

- Repository read order still P14.4 (JSON-first / on-read merchant+workspace backfill still present)
- JSON not deleted
- Dual-write not stopped
- `appendEvent` not changed
- API / Auth / Guard policy / Financial Engine / Frontend not changed
- No new migrations
- P14.13 not started

### Gates (this workstation)

| Command | Result |
| --- | --- |
| `npm test` | **302 passed / 0 failed** |
| `npm run backend:typecheck` | **pass** |
| `npx next build` | **pass** |

---

## Remaining risks / next preflight

1. Freeze a **new** JSON snapshot after stopping live traffic, copy any JSON-only rows with an explicit approved copier (not silent on-read backfill), then re-verify.
2. Align `appendEvent` identity with P14.9 sha256 **before** or as part of a later approved slice — not done here.
3. Decide how JSON-only guard rows (PG down at write time) are copied. Do not DELETE the file log to “match” PG.

---

## DO NOT (this NO-GO)

- PostgreSQL-primary read while catalogs disagree
- Auto-repair (UPDATE/DELETE/on-read backfill) to force GO
- Delete JSON
- Start P14.13 / P14.14 / P15
