# P15.3 Decision Gate — SELECT-only

**Date:** 2026-08-29  
**Scope:** P15.3 is GO. This slice **only reads** live catalog rows so each leftover decision can be classified. It does **not** start P15.4, does **not** DELETE, does **not** add FK/UNIQUE, and does **not** add migration `002`.

```text
P15.3 conservative index / concurrency    GO
   ↓
P15.3 Decision Gate                       ← this document
   ↓
        later P15.4 (not this slice — needs explicit GO per decision)
```

**Verdict: report only. No GO to delete or constrain.**

---

## Command

```bash
npm run db:p15.3-decision-gate
```

SELECT only. Writes `data/.p15.3-decision-gate`. Never INSERT/UPDATE/DELETE PostgreSQL. Do not run `db:backfill` or `db:reconcile`.

---

## Not in this slice

- DELETE of merchants, workspaces, or guard_decisions
- FK / UNIQUE / CHECK
- Migration `002_*.sql`
- API / Auth / Guard policy / Financial Engine changes
- Workspace compare-and-swap
- P15.4

---

## Live run (this workstation, 2026-08-29)

`evaluatedAt` **2026-08-29T14:23:57.535Z**. PostgreSQL was not mutated.

| Check | Result |
| --- | --- |
| Isolated P14.4–P15.3 + Decision Gate | **168/168 pass** |
| `npm run backend:typecheck` | pass |
| `npx next build` | pass (existing json-store / write-freeze tracing warnings) |
| Row counts | merchants **205**, workspaces **5**, track_events **181**, guard_decisions **167** (unchanged vs P15.3) |
| Rows deleted | **0** |
| FK / UNIQUE / 002 | **0 / 0 / none** |
| P15.4 started | **no** |

---

## Decision report

Classifications are evidence. They are **not** permission to delete or constrain.

| ID | Verdict |
| --- | --- |
| `delete-p12.2-workspaces` | **SAFE TO DELETE** |
| `delete-p125-guards` (blanket) | **BLOCKED** |
| `delete-p125-guards` (33 emails / 52 rows with no merchant) | **SAFE TO DELETE** only with a **targeted** GO |
| `unique-phone` | **BLOCKED** |
| `fk-any` | **BLOCKED** |
| `index-created-at` | **BLOCKED** |
| `workspace-cas` | **BLOCKED** |

| Label | Meaning |
| --- | --- |
| SAFE TO DELETE | Disposable leftover: no live merchant, no extra catalog refs. Still needs an explicit delete GO. |
| NEEDS REVIEW | Extra refs or format issues. Do not delete or constrain yet. |
| SAFE FOR CONSTRAINT | Current values would survive the constraint. Still needs an explicit constraint GO. |
| BLOCKED | Applying the change now would fail or drop live-account data. |

---

### 1. `p12.2-alice@test.com` / `p12.2-bob@test.com`

| Email | Workspace rows | `updated_at` | `savedAt` | files | merchant | track_events | guard_decisions | Verdict |
| --- | ---: | --- | --- | ---: | --- | ---: | ---: | --- |
| `p12.2-alice@test.com` | 1 | 2026-08-26T18:57:27.493Z | 2026-08-26T15:37:44.724Z | 1 | **no** | 0 | 0 | SAFE TO DELETE |
| `p12.2-bob@test.com` | 1 | 2026-08-26T18:57:27.493Z | 2026-08-26T15:37:44.894Z | 1 | **no** | 0 | 0 | SAFE TO DELETE |

`ownerEmail` matches the workspace PK. No other public-table refs. P12.2 API-contract leftover. **Not deleted.**

---

### 2. `p125-*` guard_decisions

P15.2 counted **orphans only** (no merchant): 33 emails / 52 rows. This gate listed **every** `p125-*@test.com` guard email.

| Slice | Emails | Rows | Merchant? | Verdict |
| --- | ---: | ---: | --- | --- |
| All `p125-*` guard emails | 93 | 155 | mixed | **BLOCKED** as a blanket delete |
| Has `merchants.email` | 60 | 103 | yes | **BLOCKED** — live-account audit |
| No merchant (the P15.2 orphan set) | 33 | 52 | no | **SAFE TO DELETE** only with a targeted GO |
| `track_events` refs | 0 | — | — | none |
| Decisions observed | allow only | 2026-08-23 → 2026-08-26 | | |

A single “delete all p125-* guards” GO would remove 103 rows that belong to 60 merchant accounts. **Do not do that.** A later targeted GO could name the 33 orphan emails only. **Nothing deleted in this slice.**

---

### 3. `UNIQUE (payload->>'phone')` — every merchant (205/205)

| Check | Count |
| ---: | ---: |
| Merchants scanned | **205** |
| Phone JSON `null` / missing | 2 (`pitch.demo.1786709548@smartprofits.dev`, `pitch.demo.1786709716@smartprofits.dev`) |
| Empty string `''` | 2 (`p124-admin-patch@test.com`, `p124-live-patch@test.com`) |
| Invalid E.164 | **0** |
| Raw duplicate groups | **1** (the two empty strings) |
| Normalized E.164 duplicate groups | **0** |
| Unique valid E.164 | **201** |

PostgreSQL UNIQUE allows multiple NULLs, so the two pitch.demo nulls would pass. Two empty strings are the **same value** and **would fail UNIQUE**. Verdict: **BLOCKED**. No UNIQUE added.

---

### 4–6. Not applied

No FK. No UNIQUE. No DELETE. No migration `002`. API/Auth/Guard/Engine unchanged. `nac-demo.json` unchanged. Event ids unchanged.

Index `created_at DESC`: still **BLOCKED** (P15.3 EXPLAIN Seq Scan cost 35.26 at 167 rows).  
Workspace CAS: still **BLOCKED** (would change `POST /api/workspace`).

---

## Isolated tests

`tests/p15.3-decision-gate.test.ts` classifies fixture rows in-process and locks SQL as read-only. It does not mock `pg.Pool`.
