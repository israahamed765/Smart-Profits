# P15.5 — Final Audit

**Date:** 2026-08-30  
**Scope:** Read-only close of the P14/P15 catalog. SELECT only. No INSERT/UPDATE/DELETE, no migration, no FK, no UNIQUE(phone), no file deletion, no P16.

```text
P15.4 Final Hardening / Verification      GO
   ↓
P15.5 Final Audit                         ← this document  GO
   ↓
        later Cleanup (operator tools)
   ↓
        later Final verification (typecheck / build / isolated tests)
   ↓
        later P16 / database close
```

**Verdict: GO.** PostgreSQL is the catalog source of truth. Counts and orphans match the P15.4 close. Catalog repositories have no JSON fallback. Schema is still `001` only. PostgreSQL was not mutated.

The project is **ready for Cleanup, then Final verification, then P16**. It is **not closed yet** — those slices need their own GO. UNIQUE(phone) and FKs remain optional and are **not** required to close.

---

## Command

```bash
npm run db:p15.5-final-audit
```

SELECT for tables, counts, orphans, constraints, migrations, indexes, payload integrity, event-id shapes, and empty phones. Writes `data/.p15.5-final-audit`. Does **not** run a write probe. Do not run `db:backfill`, `db:reconcile`, or `db:p15.4-verify` after this document exists (`db:p15.4-verify` refuses because its probe would INSERT).

---

## Not in this slice

- FK / UNIQUE(phone) / CHECK
- Migration `002_*.sql`
- Any catalog INSERT / UPDATE / DELETE
- Cleanup of P14/P15 operator tools
- Workspace compare-and-swap
- P16
- Full `npm test` against live `DATABASE_URL`
- `npx next build` (that belongs to Final verification)

---

## Live run (this workstation, 2026-08-30)

`evaluatedAt` **2026-08-30T09:18:01.220Z**. `postgresMutated` **false**. `rowsDeleted` **0**.

| Check | Result |
| --- | --- |
| Catalog counts | merchants **205**, workspaces **3**, track_events **181**, guard_decisions **115** |
| Workspace / guard / event orphans | **0 / 0 / 0** |
| Remaining workspaces | `pitch.demo.1786709548@smartprofits.dev`, `pitch.demo.1786709716@smartprofits.dev`, `tsraathmd@gmail.com` |
| Public tables | `guard_decisions`, `merchants`, `schema_migrations`, `track_events`, `workspaces` |
| FK / extra UNIQUE | **0 / none** |
| `schema_migrations` | `001_current_schema.sql` only |
| Migration `002` | **absent** |
| Merchant email / payload mismatches | **0** |
| Non-object merchant / event payloads | **0 / 0** |
| Workspace ownerEmail mismatches | **0** |
| Workspaces missing `files` array | **0** |
| `track_events.id` shapes | SHA-256 **125**, UUID **56**, other **0** |
| Empty phones | **4** (UNIQUE still blocked; optional) |
| Duplicate non-empty phones | **0** |
| Catalog repos JSON fallback | **none** (`requirePostgres` throws) |
| `demo.repository` | JSON-only `nac-demo.json` |
| `lib/utils.ts` | present |
| `docs/p15.md` / `docs/p16.md` | **absent** |
| Operator tools (`backfill` / `reconcile` / cutover CLIs) | **still on disk** (cleanup is a later GO) |

### Empty phones (deferred, not a blocker)

| Email | Note |
| --- | --- |
| `p124-admin-patch@test.com` | P15.4 already knew this row |
| `p124-live-patch@test.com` | P15.4 already knew this row |
| `pitch.demo.1786709548@smartprofits.dev` | counted here; UNIQUE still optional |
| `pitch.demo.1786709716@smartprofits.dev` | counted here; UNIQUE still optional |

P15.4 reported two empty p124 phones. This audit also counts the two pitch-demo merchants. No rows were changed. UNIQUE(phone) stays **BLOCKED** and **not required**.

### Indexes (public)

`guard_decisions_pkey`, `guard_decisions_email_created_idx`, `guard_decisions_decision_created_idx`, `merchants_pkey`, `workspaces_pkey`, `track_events_pkey`, `track_events_at_idx`, `schema_migrations_pkey`.

No created_at-only index. No phone UNIQUE. No FK.

---

## Closure status

| Question | Answer |
| --- | --- |
| Any blocker to leave P15? | **No** |
| Ready to close the database phase today? | **No — P16 has not started** |
| Next required GO | Cleanup of P14/P15 operator tools, after confirming they are unused in production/CI |
| Optional, do not add | UNIQUE(phone), FKs, migration 002 |

Still deferred (need a later GO): tool cleanup, Final verification (`backend:typecheck`, isolated tests, `next build`), P16.

---

## Isolated tests

`tests/p15.5-final-audit.test.ts` classifies fixture evidence in-process and fakes SELECT-only replies. It does not import a live `pg.Pool`. After this document exists, `tests/p15.4-final-hardening.test.ts` requires it, and `db:p15.4-verify` refuses to re-run the write probe.

Isolated P15.1–P15.5: **24/24 pass**. `npm run backend:typecheck`: pass. `next build` was not run (Final verification, later GO).
