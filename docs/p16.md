# P16 — Final Closure

**Date:** 2026-08-30  
**Scope:** Official close of the PostgreSQL / P14 / P15 catalog migration. Documentation and a local marker only. No catalog writes. No schema change. No P17.

```text
P15.7 Final Verification                  GO
   ↓
P16 Final Closure                         ← this document  GO — FULLY CLOSED
```

**P16 STATUS: GO — FULLY CLOSED**

PostgreSQL is the source of truth for the four catalogs. The migration, hardening, cleanup, and verification sequence is complete. This slice did not connect to PostgreSQL and did not mutate it.

---

## Source of truth

| Store | Role |
| --- | --- |
| PostgreSQL `merchants` | accounts |
| PostgreSQL `workspaces` | workspaces |
| PostgreSQL `track_events` | track events |
| PostgreSQL `guard_decisions` | Smart Guard decisions |
| `nac-demo.json` via `demo.repository` | **only** remaining JSON store; NAC demo flags, not a catalog |

The four catalog repositories (`user`, `workspace`, `event`, `guard-log`) use `requirePostgres` and do not import `json-store`. There is no JSON fallback. Catalog JSON files were removed in P14.17–P14.23. Reintroducing them is out of scope.

---

## Final catalog state

Recorded at Final Verification (2026-08-30T09:41:56.016Z) and unchanged by this slice:

| Table | Count |
| --- | ---: |
| merchants | **205** |
| workspaces | **3** |
| track_events | **181** |
| guard_decisions | **115** |

| Check | Result |
| --- | --- |
| Workspace / guard / event orphans | **0 / 0 / 0** |
| Payload mismatches (merchant email, workspace ownerEmail, non-object payloads, missing files) | **0** |
| Remaining workspaces | `pitch.demo.1786709548@smartprofits.dev`, `pitch.demo.1786709716@smartprofits.dev`, `tsraathmd@gmail.com` |

---

## Schema (intentional)

- Applied migration: **`001_current_schema.sql` only**
- Migration `002` : **absent**
- Foreign keys: **not added** — data is clean (0 orphans); an FK would be a new schema change, not required to close
- `UNIQUE(phone)`: **not added** — blocked by empty phones and **not required** to close
- `track_events.id`: **125** historical SHA-256 + **56** new UUIDs from `appendEvent` (`randomUUID()`). Do not unify.

---

## Cleanup

P14/P15 cutover tools (backfill, reconcile, shadow, freeze/unfreeze, cutover/archive) were removed in P15.6 after a reference audit showed they were unused in production, CI, and current operational scripts. They must not be restored.

Kept: `postgres.ts`, `migrate` + `001`, P15.1–P15.5 lock modules, `write-freeze.ts` (Smart Guard), `json-store` for `nac-demo.json` only, `lib/utils.ts`.

---

## Final Verification (P15.7)

| Gate | Result |
| --- | --- |
| Isolated P14/P15 tests | **106/106** |
| `npm run backend:typecheck` | **PASS** |
| `npx next build` | **PASS** |
| Smoke (GET-only API + INSERT probe **ROLLBACK**) | **PASS** |
| `postgresMutated` | **false** |

---

## Marker

`data/.p16-final-closure` records this close. Creating it does not write to PostgreSQL.

---

## Not in this slice

- Any INSERT / UPDATE / DELETE on PostgreSQL
- FK / UNIQUE(phone) / migration `002`
- Restoring JSON catalogs or deleted operator tools
- API / Auth / Guard / Engine changes
- P17 or further database work
