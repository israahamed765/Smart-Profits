# P14.14 — Final Consistency Verification

**Date:** 2026-08-26  
**Scope:** SELECT-only confirmation that P14.13 left catalogs consistent. **JSON remains primary. Read order unchanged. Dual-write not retired. `appendEvent` still `randomUUID()`. No PostgreSQL-primary. No P14.15. Do not re-run `db:reconcile`.**

```text
P14.13 Reconciliation     GO (locked)
   ↓
P14.14 Final Verification ← this document
   ↓
        PASS → later P14.15 read cutover (not this slice)
        NO-GO → stop; do not auto-repair
```

---

## Command

```bash
npm run db:verify-final
```

This is **not** `db:verify` (P14.11, which may copy). This is **not** `db:reconcile`. Any mismatch is NO-GO.

---

## Forbidden

- INSERT / UPDATE / DELETE
- Changing repositories or `appendEvent`
- PostgreSQL-primary / stopping dual-write
- Modifying JSON
- Copying the 6 PG-only Guard rows into JSON
- Deleting those 6 rows

---

## Intentional exception

P14.13 retained six Guard IDs in PostgreSQL only (`retain_in_postgres_not_in_json_snapshot`). P14.14 treats **exactly that set** as documented extras. Any other PG-only or any JSON-only id is an unexpected conflict.

Locked ids: `server/db/intentional-pg-only-guards.ts`.

---

## Event identity

A PG row may keep a dual-write UUID if its **payload** hashes to a JSON event. Duplicate payload hashes, unmatched UUID payloads, or missing JSON payloads are unexpected conflicts. Ids are not rewritten.

---

## Gate

```text
FINAL VERIFICATION
==================
Merchants:        PASS
Workspaces:       PASS
Track Events:     PASS
Guard Decisions:  PASS
Schema:           PASS
Migration:        PASS
JSON Integrity:   PASS
Event Identity:   PASS
Intentional PG-only guards: 6 documented
Unexpected conflicts: 0
```

JSON integrity: live `data/` fingerprints unchanged across the SELECT pass.

---

## Live run (this workstation, 2026-08-26)

`npm run db:verify-final` ran SELECT-only. **Did not** run `db:reconcile`. JSON catalogs were not written by the verifier.

```text
FINAL VERIFICATION
==================
Merchants:        FAIL
Workspaces:       FAIL
Track Events:     PASS
Guard Decisions:  FAIL
Schema:           PASS
Migration:        PASS
JSON Integrity:   PASS (first pass; a later pass saw live writes mid-SELECT)
Event Identity:   PASS
Intentional PG-only guards: 6 documented
Unexpected conflicts: 4
```

| | JSON | PG |
| --- | ---: | ---: |
| merchants | 201 | 201 — **1 payload conflict** (`p123-1787756385394-18@test.com`) |
| workspaces | 5 | 5 — **2 payload conflicts** (`p12.2-alice@test.com`, `p12.2-bob@test.com`) |
| track_events | 135 | 135 — 125 sha256 + 10 UUID covering JSON payloads; 0 duplicates |
| guard_decisions | 147 | 152 — documented 6 PG-only still present; **3 JSON-only**; **2 unexpected PG-only** |

The six P14.13 ids are still in PG (`missingDocumented: []`). New drift after unfreeze (product writes resumed) is why this is not PASS.

**Verdict: NO-GO.** Do not start P14.15. Do not auto-repair from this slice.

---

## DO NOT

- P14.15 PostgreSQL-primary read cutover
- Re-run `db:reconcile` to hide a mismatch
