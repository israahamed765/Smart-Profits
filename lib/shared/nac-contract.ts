/**
 * P9: NAC contracts live in `@/shared/contracts/nac-contract`.
 * Client-safe re-export — no API keys (those stay in server/smart-guard/nac-env.ts).
 */
export * from "@/shared/contracts/nac-contract";
